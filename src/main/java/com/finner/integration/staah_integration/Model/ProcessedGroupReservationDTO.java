package com.finner.integration.staah_integration.Model;

public class ProcessedGroupReservationDTO {
    /*
    * in this casse first we need a group reservation id from pms endpoint
    * then with all those reervations we just have to send the group id only and the overall code if going to be the same
    * so i don't think a whole new code logic is required we can utlise the existing one
    * */

}
